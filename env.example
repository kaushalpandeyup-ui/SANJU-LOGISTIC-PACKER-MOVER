# Copy this file to .env and fill in values for production
# SMTP settings (optional). If not provided, the server will use Ethereal for dev emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# JWT secret - set this in production
JWT_SECRET=dev-secret

PORT=3000
